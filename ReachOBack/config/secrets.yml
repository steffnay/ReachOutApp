# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rails secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

# Shared secrets are available across all environments.

# shared:
#   api_key: a1B2c3D4e5F6

# Environmental secrets are only available for that specific environment.

development:
  secret_key_base: 246173fd95292256032638ade5d7647ccce76d6f95d6e83094eb75f7d532c7c5001dd5aa338f470d34c8bdda56af7486da7f5b4c4dc267aa84d5df1c4b90f370
  twilio_account_sid:  <%= ENV.fetch("TWILIO_ACCOUNT_SID", "AC4fc26f8a6b43be71dd74a92926caefb9") %>
  twilio_auth_token:  <%= ENV.fetch("TWILIO_AUTH_TOKEN", "ddfb2b8562dabde739e938b98448c278") %>
  twilio_phone_number:  <%= ENV.fetch("TWILIO_PHONE_NUMBER", "+12062079578") %>

test:
  secret_key_base: 8ec435d995dedaeaffb06ad4c0b28c36162d05bae4b4650381e5743faa65578a6e670f2ae4d183cda05917faa1f35c9da77142a26f98f21914454c6fb649b822
  twilio_account_sid:  <%= ENV.fetch("TWILIO_ACCOUNT_SID", "AC4fc26f8a6b43be71dd74a92926caefb9") %>
  twilio_auth_token:  <%= ENV.fetch("TWILIO_AUTH_TOKEN", "ddfb2b8562dabde739e938b98448c278") %>
  twilio_phone_number:  <%= ENV.fetch("TWILIO_PHONE_NUMBER", "+12062079578") %>
# Do not keep production secrets in the unencrypted secrets file.
# Instead, either read values from the environment.
# Or, use `bin/rails secrets:setup` to configure encrypted secrets
# and move the `production:` environment over there.
production:
  secret_key_base: "testing"
